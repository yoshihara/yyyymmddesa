{
  "name": "yyyymmddesa",
  "version": "0.1.0",
  "description": "Tiny tools for daily report in esa.io",
  "main": "content.js",
  "private": false,
  "scripts": {
    "package": "yarn build && zip package.zip build/*",
    "clean": "rm -rf build/ && mkdir -p build",
    "build:js": "webpack --config webpack.config.js",
    "build:metadata": "rm -rf build/*.png build/manifest.json && cp -a metadata/* build/",
    "build:html": "rm -rf build/*.html && pug -s src/pug -o build",
    "build:css": "rm -rf build/*.css && node-sass -q src/sass -o build",
    "build": "npm-run-all clean build:*",
    "watch:js": "watchy -w src/js -w webpack.config.js -- yarn run build:js",
    "watch:metadata": "watchy -w metadata -- yarn run build:metadata",
    "watch:html": "watchy -w src/pug -- yarn run build:html",
    "watch:css": "watchy -w src/sass -- yarn run build:css",
    "watch": "npm-run-all clean -p watch:*",
    "fmt": "prettier --write src/js/**/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yoshihara/yyyymmddesa"
  },
  "keywords": [
    "esa.io"
  ],
  "author": "yoshihara",
  "license": "MIT",
  "homepage": "https://github.com/yoshihara/yyyymmddesa",
  "devDependencies": {
    "@babel/core": "7.0.0-beta.40",
    "@babel/plugin-transform-runtime": "^7.0.0-beta.40",
    "@babel/preset-env": "^7.0.0-beta.40",
    "@babel/runtime": "^7.0.0-beta.40",
    "babel-loader": "8.0.0-beta.0",
    "expose-loader": "^0.7.3",
    "node-sass": "^4.5.3",
    "npm-run-all": "^4.0.2",
    "prettier": "^1.11.1",
    "pug-cli": "^1.0.0-alpha6",
    "watchy": "^0.7.0",
    "webpack": "^3.5.4",
    "css-loader": "^0.28.10",
    "style-loader": "^0.20.3"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "moment": "^2.21.0"
  }
}
