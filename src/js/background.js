// manifest.jsonで `"default_popup": "index.html"` を指定した時と同じ動作をする書き方↓

// chrome.browserAction.onClicked.addListener(function(tab) {
//     chrome.browserAction.setPopup({popup: "index.html"});
// });

